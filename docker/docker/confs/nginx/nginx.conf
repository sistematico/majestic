#################################
##### LOCALHOST             #####
#################################
server {
    listen 80;
    listen [::]:80;
    server_name localhost;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 5m;

    ssl_certificate      /etc/ssl/nginx/localhost.pem;
    ssl_certificate_key  /etc/ssl/nginx/localhost-key.pem;

    autoindex on;
    fastcgi_intercept_errors on;
    disable_symlinks off;

    index index.php index.html;
    server_name localhost;

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;

    root /code/wordpress;

    include snippets/wordpress.conf;
}

#################################
##### LOCALHOST             #####
#################################
server {
    listen 80;
    listen [::]:80;
    server_name pdvlaravel;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 5m;

    ssl_certificate      /etc/ssl/nginx/pdvlaravel.pem;
    ssl_certificate_key  /etc/ssl/nginx/pdvlaravel-key.pem;

    autoindex on;
    fastcgi_intercept_errors on;
    disable_symlinks off;

    index index.php index.html;
    server_name localhost;

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;

    root /code/pdvlaravel;

    include snippets/laravel.conf;
}

#################################
##### STARTPAGE             #####
#################################
server {
    listen 80;
    listen [::]:80;
    server_name startpage;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 5m;

    ssl_certificate      /etc/ssl/nginx/startpage.pem;
    ssl_certificate_key  /etc/ssl/nginx/startpage-key.pem;

    autoindex on;
    fastcgi_intercept_errors on;
    disable_symlinks off;

    index index.php index.html;
    server_name startpage;

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;

    root /code/startpages/gaegu;

    include snippets/php.conf;
}

#################################
##### PAGSEGURO             #####
#################################
server {
    listen 80;
    listen [::]:80;
    server_name pagseguro;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 5m;

    ssl_certificate      /etc/ssl/nginx/pagseguro.pem;
    ssl_certificate_key  /etc/ssl/nginx/pagseguro-key.pem;

    autoindex on;
    fastcgi_intercept_errors on;
    disable_symlinks off;

    index index.php index.html;
    server_name pagseguro;

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;

    root /code/pagseguro-php;

    include snippets/slim.conf;
}

#################################
##### MAJESTIC              #####
#################################
server {
    listen 80;
    listen [::]:80;
    server_name majestic;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 5m;

    ssl_certificate      /etc/ssl/nginx/majestic.pem;
    ssl_certificate_key  /etc/ssl/nginx/majestic-key.pem;

    autoindex on;
    fastcgi_intercept_errors on;
    disable_symlinks off;

    index index.php index.html;
    server_name majestic;

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;

    root /code/celke;

    include snippets/php.conf;
}

#################################
##### MUDANCAONLINE         #####
#################################
server {
    listen 80;
    listen [::]:80;
    server_name mudancaonline;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 5m;

    ssl_certificate      /etc/ssl/nginx/mudancaonline.pem;
    ssl_certificate_key  /etc/ssl/nginx/mudancaonline-key.pem;

    autoindex on;
    fastcgi_intercept_errors on;
    disable_symlinks off;

    index index.php index.html;
    server_name mudancaonline 127.0.0.3;

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;

    root /code/mudancaonline/public;

    include snippets/mvc.conf;
}

#################################
##### LUCASBRUM             #####
#################################
server {
    listen 80;
    listen [::]:80;
    server_name lucasbrum;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 5m;

    ssl_certificate      /etc/ssl/nginx/lucasbrum.pem;
    ssl_certificate_key  /etc/ssl/nginx/lucasbrum-key.pem;

    autoindex on;
    fastcgi_intercept_errors on;
    disable_symlinks off;

    index index.html;
    server_name lucasbrum;

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;

    root /code/lucasbrum;

    include snippets/vue-simple.conf;
}

#################################
##### SOMDOMATO             #####
#################################
server {
    listen 80;
    listen [::]:80;
    server_name somdomato;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 5m;

    ssl_certificate      /etc/ssl/nginx/somdomato.pem;
    ssl_certificate_key  /etc/ssl/nginx/somdomato-key.pem;

    autoindex on;
    fastcgi_intercept_errors on;
    disable_symlinks off;

    index index.php index.html;
    server_name somdomato;

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;

    root /code/somdomato/public;

    include snippets/mvc.conf;
}

#################################
##### RADIOCHAT             #####
#################################
server {
    listen 80;
    listen [::]:80;
    server_name radiochat;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 5m;

    ssl_certificate      /etc/ssl/nginx/radiochat.pem;
    ssl_certificate_key  /etc/ssl/nginx/radiochat-key.pem;

    autoindex on;
    fastcgi_intercept_errors on;
    disable_symlinks off;

    index index.php index.html;
    server_name radiochat;

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;

    root /code/radiochat;

    include snippets/php.conf;
}

#################################
##### ADBLOCK               #####
#################################
server {
    listen 443 ssl;
    listen [::]:443 ssl;

    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 5m;

    ssl_certificate      /etc/ssl/nginx/127.0.0.2.pem;
    ssl_certificate_key  /etc/ssl/nginx/127.0.0.2-key.pem;

    server_name 127.0.0.2;
    return 301 http://$host$request_uri;
}

server {
    listen 80;
    listen [::]:80;

    autoindex on;
    fastcgi_intercept_errors on;
    disable_symlinks off;

    index index.php index.html;
    server_name 127.0.0.2;

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;

    root /code/adblock;

    include snippets/php.conf;
}
