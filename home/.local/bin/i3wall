#!/usr/bin/env bash

OIFS="$IFS"
IFS=$'\n'
if [ $1 ]; then
    type="$(file -ib "$1" | awk -F'/' '{print $1}' 2> /dev/null)"
    if [ "$type" == "image" ]; then
        /usr/bin/feh --bg-fill "$1"
    fi
fi
IFS="$OIFS"
